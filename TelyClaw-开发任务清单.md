# TelyClaw 开发任务清单

> 配套文档：`TelyClaw-产品文档.md`
> 使用方式：按阶段顺序做，每个任务都带有**验收标准**——做完能自己在界面上确认才算完成。
> 本清单不含工期估算（题面未限定时长）。

---

## 阶段一：骨架跑通

**目标**：能在浏览器里打开页面，按账号和窗口取出一批帖子并列表展示。

| # | 任务 | 验收标准 |
|---|---|---|
| 1.1 | 建项目目录，写 `requirements.txt`（只装：web 框架、网络请求库） | 能在独立虚拟环境里安装成功 |
| 1.2 | 写 `app.py`：启动服务，浏览器打开本地地址能看见页面 | 一条命令启动成功，页面不报错 |
| 1.3 | 写 `config.py`：集中管理配置项（AI 密钥、取数方式、默认窗口、端口） | 改配置不需要动业务代码 |
| 1.4 | 定义数据模型：`Account`、`Post`（作者/正文/时间/点赞/转发/回复/引用） | 字段与产品文档 2.1 节一致 |
| 1.5 | 写 `store/json_store.py`：读写本地 JSON 文件 | 写入后重启服务数据还在 |
| 1.6 | 写 `sources/base.py`：定义取数接口 `fetch(账号列表, 起始时间, 结束时间)` | 接口签名固定下来 |
| 1.7 | 造 `data/seed_posts.json`：约 70~90 条帖子模板，**只记"距今多少分钟"（相对偏移），不记绝对时间** | 文件里搜不到任何写死的日期 |
| 1.8 | 写 `sources/mock_source.py`：按相对偏移换算成真实时间生成帖子 | 任何时候运行，1h/4h/8h/24h/3d 五个窗口都查得到数据 |
| 1.9 | 造 6 个预置 KOL 账号（头像/粉丝数/显示名），写入 `accounts.json` | 设置页能看到 6 个 |
| 1.10 | 前端页面骨架：顶部 4 个 Tab + 一个能显示内容的区域 | Tab 可切换，不报错 |
| 1.11 | 设置页：账号列表 + 添加/删除/启用开关，保存后落盘 | 添加一个账号 → 刷新页面 → 还在 |
| 1.12 | 监控页：账号多选卡片 + 窗口按钮排（1h/4h/8h/24h/3d）+ "立即检测"按钮 | 勾几个账号、选窗口、点检测 |
| 1.13 | 打通检测接口：返回过滤后的帖子列表并在页面上展示 | 切换窗口，列表条数明显变化 |

**阶段一出口检查**：选"1 小时"能出帖子，选"3 天"出的条数明显更多。

---

## 阶段二：热点引擎（不接 AI）

**目标**：从帖子列表算出话题榜单。此阶段话题名先用高频关键词代替。

| # | 任务 | 验收标准 |
|---|---|---|
| 2.1 | `core/keywords.py`：转小写、去停用词、保留全大写缩写、统计词频与相邻词组合 | 能从一批帖子里抽出像样的关键词 |
| 2.2 | 同上：把 `#话题标签` 作为强信号标签单独提取 | 带 # 的词优先成为标签 |
| 2.3 | `core/cluster.py`：按共现重合度贪心合并标签成簇 | 讲同一件事的帖子进了同一个簇 |
| 2.4 | 丢弃成员数 < 2 的簇（去噪） | 孤零零的帖子不单独成话题 |
| 2.5 | `core/scoring.py`：四个维度分别做窗口内归一化到 0~100 | 每个话题四个维度都有分 |
| 2.6 | 同上：新鲜度按 `e^(-时间/τ)` 加权，τ = 窗口长度 ÷ 3 | 新帖多的分数明显更高 |
| 2.7 | 同上：按 35/25/20/20 权重求和得热度分 | 公式与产品文档 2.4 节一致 |
| 2.8 | `core/pipeline.py`：串起 过滤 → 抽词 → 聚类 → 打分 → 排序 | 一次调用返回完整榜单 |
| 2.9 | 榜单 UI：排名 + 话题名 + 热度分 + 四维数值小条 | 能看出第一名为什么是第一 |
| 2.10 | 界面常驻标注"当前窗口：过去 X" | 任何时刻都能看见当前是哪个窗口 |
| 2.11 | **专项验证：1 小时窗口能否聚出 ≥3 个话题** | 若不足，回头调 1.8 的模拟数据分布（这是硬要求） |

**阶段二出口检查**：不联网、不填密钥，也能出一版完整的排行榜。

---

## 阶段三：接入 AI（DeepSeek）

**目标**：话题命名与摘要、排名理由、Top 精选、内容角度、写稿全部由 AI 产出。

| # | 任务 | 验收标准 |
|---|---|---|
| 3.1 | `ai/client.py`：封装调用，含超时与错误规范化 | 密钥错误/断网都能拿到明确错误类型 |
| 3.2 | 统一错误结构：是否成功 / 错误码 / 人话说明 / 是否可重试 | 见产品文档 6.3 节表格 |
| 3.3 | `ai/prompts.py`：写 5 个 Prompt（话题摘要、排名理由、Top 精选、内容角度、写稿） | 每个 Prompt 明确要求返回固定结构 |
| 3.4 | 任务「识别话题 + 摘要」：一次请求处理所有粗簇 | 话题名是人话，不再是一堆关键词 |
| 3.5 | 任务「排名理由」：输入各维度数值，输出一句为什么 | 第一名的理由听起来具体、可信 |
| 3.6 | 任务「Top 精选」：输入榜单 + TelyClaw 定位，输出 3 个 + 契合度理由 | 推荐理由能体现"为什么值得 TelyClaw 跟进" |
| 3.7 | 设置页加「TelyClaw 账号定位」输入框，默认值填好 | 不填也有默认定位可用 |
| 3.8 | 设置页加 AI 密钥输入框，落盘保存 | 填一次，重启后还在 |
| 3.9 | 前端错误提示：顶部横幅 + 重试按钮 | 密钥为空时也能正常看到提示，不白屏 |
| 3.10 | 密钥为空时的常驻引导条 + 一键跳设置页 | 一进监控页就看得见 |
| 3.11 | 解析失败自动重试 1 次，仍失败才提示用户 | 偶发格式问题用户无感 |
| 3.12 | 加载状态：每次 AI 调用都有明确的"正在生成"提示 | 不会出现点了没反应的假死感 |

**阶段三出口检查**：断网时点检测，页面给出人话提示 + 重试按钮，其余部分照常可用；恢复网络点重试能出结果。

---

## 阶段四：工作台与发布

**目标**：从热点一路走到"发布完成"，并全部持久化。

| # | 任务 | 验收标准 |
|---|---|---|
| 4.1 | 工作台页：左侧 Top 精选热点列表，右侧工作区 | 点热点能切换 |
| 4.2 | 点热点 → 出 3 个内容角度卡片（标题 + 为什么有价值） | 三个角度明显不重复 |
| 4.3 | 点某个角度 → 出 3 条不同风格帖子（犀利观点/数据清单/提问互动） | 三条读起来风格确实不同 |
| 4.4 | 选一条进入编辑框，可直接改文字 | 改完内容被保留 |
| 4.5 | 编辑框实时字数 + 280 字上限提示，超限变色 | 超过 280 字有明显警示 |
| 4.6 | 草稿落盘（`drafts.json`），刷新页面草稿还在 | 关掉浏览器再打开，草稿还在 |
| 4.7 | "生成更多"按钮：可再要一批帖子候选 | 点了能出新内容 |
| 4.8 | `Publisher` 接口 + Mock 实现 | 接口定义清晰，换真发只改实现 |
| 4.9 | 点"发布" → 弹出 X 样式预览卡片（头像/名字/正文/时间） | 预览看得出来这条发出去长什么样 |
| 4.10 | 确认发布 → 状态变"已发布" + 写入 `published.json` + 文案复制到剪贴板 | 可以去任意输入框粘贴验证 |
| 4.11 | 发布记录页：草稿 / 已发布 分组，显示正文、关联热点、角度、时间 | 刚才发的那条在"已发布"里 |
| 4.12 | 已发布条目提供"复制"按钮 | 点一下能粘出来 |

**阶段四出口检查**：完整走一遍"选热点 → 选角度 → 选帖子 → 改字 → 发布 → 在记录页看到"，刷新页面后记录仍在。

---

## 阶段五：演示打磨

**目标**：确保演示当天每一步都稳、都好看、都讲得清。

| # | 任务 | 验收标准 |
|---|---|---|
| 5.1 | 造数据复核：五个窗口各自能聚出 ≥3 个话题 | 逐个窗口点一遍确认 |
| 5.2 | "换一批数据"按钮：重新随机生成互动数与措辞 | 演示内容不会每次完全一样 |
| 5.3 | 界面全程标注"演示数据 · 内容为 AI 模拟生成，非真实言论" | 每个展示帖子内容的位置都可见 |
| 5.4 | 空状态处理：没勾账号、没数据、AI 未配置三种情况都有友好文案 | 不会出现空白页面 |
| 5.5 | 窗口切换对比效果：切 1h 和 3d 榜单差异要明显 | 演示时能一眼看出区别 |
| 5.6 | 按产品文档第 8 节演示脚本完整走查 3 遍 | 3 遍都不卡死、不报错 |
| 5.7 | 检查所有按钮都有响应，没有点了没反应的 | 逐个点一遍 |
| 5.8 | 启动说明：写一份"怎么把它跑起来"的步骤（面向非程序员） | 照着做一次能成功 |

---

## 附：两处最容易翻车的地方（务必优先验证）

1. **1 小时窗口空数据** —— 模拟数据若写死绝对时间，换个时间演示就会全空。必须在阶段一 1.7/1.8 就用相对偏移，并在阶段二 2.11 专项验证。
2. **AI 失败导致演示中断** —— 用户明确选择"不做兜底内容"，所以必须把"友好提示 + 一键重试"做扎实，且保证非 AI 部分（取数/聚类/打分/榜单）完全不依赖 AI 也能展示。
