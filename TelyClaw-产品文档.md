# TelyClaw 产品文档（MVP）

> 一句话定位：**TelyClaw 是一个"盯人 → 抓热点 → AI 帮你写成能发的帖子"的 X 内容雷达。**
>
> 本文是 MVP 的完整规划。当前版本**不接入真实 X API**，但取数层做了接口抽象，将来填入 X 的凭证即可无缝切换到真实数据。

---

## 0. 本文档的目标读者与边界

- **目标**：交付一个可在面试/演示场景下从头点到尾、不卡死、不报错的可运行 MVP。
- **非目标**（本次明确不做）：多用户、登录鉴权、云端部署、真实抓取、定时调度、生产级性能。
- **题面未限定时长**，因此本文档只划分阶段顺序，**不给工期估算**。

---

## 1. 决策快照（已逐项确认，不再变动）

| # | 决策点 | 最终结论 |
|---|---|---|
| 1 | MVP 定位 | **演示优先**。保证端到端跑通与说服力，不为真实可用性牺牲演示稳定性 |
| 2 | 运行环境 | **本地 Python 服务 + 网页**，一条命令启动，浏览器打开本地地址 |
| 3 | AI 能力 | **纯真 LLM（DeepSeek，写死）**，不做离线兜底生成 |
| 4 | 错误处理 | LLM 失败时**友好人话提示 + 一键重试**，不白屏、不卡死、不用假内容顶替 |
| 5 | 数据来源 | **三路并存**：内置模拟数据 / 手动粘贴导入 / 预留真 X API，共用同一个取数接口 |
| 6 | 持久化 | **全部落本地文件**：账号、设置、草稿、发布记录，刷新和重启都不丢 |
| 7 | 热点定义 | **关键词/话题聚类**：同主题的帖子聚成一类，一类 = 一个热点 |
| 8 | 内容安全 | **AI 逐条判定 7 类风险 → 只打标记不删帖，话题保留，由人决定**；未检查时必须明说 |
| 9 | 热度公式 | **九维加权**：互动 20% + 账号 20% + 提及 15% + 新鲜度 15% + 升温 10% + 玩梗 5% + 搞笑 5% + Drama 5% + 未饱和 5%（后三项 AI 打分） |
| 10 | 数量结构 | 热点全集 **6~12 个** → 排行榜 → **Top 精选 3 个** |
| 11 | 精选方式 | **AI 推荐 + 用户可改**（可取消、可换成榜上其他热点） |
| 12 | 内容角度 | 每个重点热点出 **3 个角度**，点其中一个再生成帖子 |
| 13 | 帖子候选 | 每个角度出 **3 条不同风格**，选一条进入编辑框 |
| 14 | 发布语义 | **模拟发布**：预览卡片 → 确认 → 写入本地已发布列表 + 复制到剪贴板；发布器抽象成接口，填凭证即可真发 |
| 15 | 页面结构 | **顶部 4 个 Tab**（设置 / 监控 / 内容工作台 / 发布记录），主流程内部用步骤条串联 |
| 16 | 预置账号 | **6 个真实科技/AI 领域 KOL 账号名**，界面全程标注"演示数据 · 内容为 AI 模拟生成，非真实言论" |
| 17 | 触发方式 | **手动点"立即检测"**，不做后台定时轮询 |
| 18 | 前端技术 | **零构建**：原生 HTML + CSS + JS，不需要 npm 安装和打包 |

---

## 2. 核心概念与口径定义（全文最不能含糊的一节）

### 2.1 四个基本对象

| 对象 | 是什么 | 关键字段 |
|---|---|---|
| **Account（监测账号）** | 你在设置页添加的 X 账号 | 用户名、显示名、头像、粉丝数、是否启用 |
| **Post（帖子）** | 一次检测抓回来的单条内容 | 作者、正文、发布时间、点赞/转发/回复/引用数 |
| **Topic（热点）** | 若干条讲同一件事的帖子聚成的一类 | 话题名、关键词列表、成员帖子、热度分、排名理由 |
| **Draft（草稿/帖子）** | 基于某个热点的某个角度生成的待发内容 | 正文、风格、关联热点、状态（草稿/已发布） |

### 2.2 三层结构（题面里混在一起说的，必须拆开）

```
第一层：热点全集  →  第二层：排行榜      →  第三层：Top 精选
6~12 个话题          按热度分从高到低       3 个"最值得 TelyClaw 跟进"
（AI 识别+命名）     （带"为什么排第一"）   （AI 推荐，用户可改）
```

### 2.3 热点是怎么算出来的（两阶段：先确定性，再交给 AI）

**阶段 A — 确定性处理（不调 AI，保证每次可复现）**

1. 按时间窗口过滤帖子：只保留 `发布时间 ≥ 现在 - 窗口长度`
2. 关键词抽取：
   - `#话题标签` 视为强信号，直接作为候选标签
   - 英文正文：转小写、去停用词（the/is/and/...）后统计词频，同时统计相邻两词组合（bigram）
   - 保留全大写缩写（AI、LLM、MCP、RAG、API 等）不转小写
   - 取频次 Top N 作为候选标签
3. **两级聚类**（`core/cluster.py`，阈值都在文件顶部）：
   - **一级（主信号）**：帖子里 hashtag 够多时（≥3 个不同标签且覆盖 ≥50% 的帖子），**直接用 hashtag 聚类**。
     标签是作者自己给出的"这条在讲什么"，信号最干净、结果最可预期。
     两个标签的共现重合度 = 交集帖子数 ÷ 较少那个的帖子数，≥0.5 就并成一簇。
   - **二级（兜底）**：hashtag 不够时（粘贴导入的帖子常常没有标签），退回**关键词共现聚类**：
     按标签分数排序取种子，新标签的帖子被已有簇覆盖 ≥75% 就并入；
     一个簇 ≥70% 的帖子都在更大的簇里就丢掉（去重）；最终上限 12 个话题。
   - 踩过的坑：早期只用二级策略，通用词（model、nobody）横跨多个话题，把簇全粘在一起，
     8h 窗口炸出 51 个话题、名字还是 "nobody" 这种噪音词 —— 所以才改成两级。
4. 丢弃成员帖子数 < 2 的簇（噪声），并按帖子数排序、截断到 12 个

**阶段 B — AI 处理（DeepSeek）**

把阶段 A 产出的所有粗簇（每个簇带：高频词、3~5 条代表帖子、统计数字）打包成**一次**请求发给 DeepSeek，让它输出：

- 是否把某几个簇合并 / 某个簇拆分
- 每个最终话题的**名字**（人能看懂的短标题）
- 每个话题的**一句话摘要**

> **设计说明**：聚类本身用确定性算法而非 LLM，是为了让"同一个输入永远得到同一个结果"，避免演示时刷新一下页面榜单就变了。但"识别出这是几个话题、叫什么名字、怎么概括"这一层判断确实由 AI 完成，符合"AI 自动识别多个话题/热点"的要求。

### 2.4 内容安全检查（进榜单之前先过一遍）

AI 逐条判定窗口内的每一条帖子，命中的**只打标记、不删帖** —— 话题保留在榜单上，跟不跟由人决定。

**7 类风险类别**（`config.py` 的 `RISK_CATEGORIES`，可增删）：

| # | 类别 |
|---|---|
| 1 | 政治立场与选举 |
| 2 | 仇恨或歧视 |
| 3 | 暴力与灾难消费 |
| 4 | 色情内容 |
| 5 | 针对个人的恶意攻击 |
| 6 | 未经证实的严重指控 |
| 7 | 明显违法或欺诈内容 |

**实现口径：**

- **纯 AI 判断，不用关键词黑名单** —— 黑名单漏判率高，也会把正常的技术争论误杀
- 一次调用批量判定窗口内**全部**帖子（每批 50 条），不是逐条请求
- 明确要求"只判明确命中的"，正常的产品吐槽、商业批评、行业分歧不算风险，避免过度敏感
- 命中的帖子：话题卡片顶部显示 `⚠️ 这个话题里有 N 条被判为风险内容：xxx`，风险帖本身标红
- **话题不删除**，只是不参与 AI 的 Top 推荐（prompt 里写明 `risk_count > 0` 一律不选）
- **三种状态必须如实显示**，不许假装检查过：

| 状态 | 界面显示 |
|---|---|
| `ok` 且命中 | ✅ 已用 AI 检查 N 条帖子，标记出 M 条风险内容、涉及 K 个话题 |
| `ok` 且没命中 | ✅ 已用 AI 检查 N 条帖子，未发现风险内容 |
| `failed` / `unchecked` | ⚠️ **未执行安全检查** —— 本榜单没有任何风险标记，别直接拿去发帖 |

- `exclude_risky` 开关（默认 `false`）：改成 `true` 才真正把风险帖从聚类中剔除，满足"真要排除"的场景

### 2.5 热度分公式（九维加权）

九个维度先各自在**当前窗口内**做归一化（最高的那个得 100 分，最低的得 0 分），再加权求和：

| 维度 | 权重 | 谁算 | 计算方式 | 为什么要有它 |
|---|---|---|---|---|
| **互动总量** | 20% | 规则 | 点赞 + 转发 + 回复 + 引用 之和 | 区分"有人聊"和"聊得很热" |
| **参与账号数** | 20% | 规则 | 有多少个**不同的**账号在聊 | **防止一个人刷屏霸榜** |
| **提及量** | 15% | 规则 | 该话题下的帖子条数 | 最直接的"有多少人在聊" |
| **新鲜度** | 15% | 规则 | 按互动量加权的平均时间新鲜度 | 让突发话题冒头，不被慢热话题占死 |
| **升温程度** | 10% | 规则 | 最近 1/4 窗口的提及占比 ÷ 25% | 抓"正在起来"的，而不只是"已经很大"的 |
| **玩梗潜力** | 5% | AI | 模型打 0~100 分 | 能不能被做成梗、段子、二创 |
| **搞笑度** | 5% | AI | 模型打 0~100 分 | 有没有幽默、反差、荒诞感 |
| **Drama 程度** | 5% | AI | 模型打 0~100 分 | 观点分歧和火药味（越强越容易引发回复转发） |
| **未饱和机会** | 5% | 规则 | 账号数 ÷ 提及数 × 100 | 人多帖少 = 刚扩散开还没被消费完 |

**几个容易踩的坑，这里定死：**

- **内容性三维度（玩梗/搞笑/Drama）由 AI 打分，所以必须在拿到 AI 结果后再算一次分**，否则这 15% 权重等于没生效（实现里 `pipeline` 在 `label_topics` 之后重新调用 `score_topics`）
- **AI 没参与时，三维度取中性值 50**。所有话题取值相同 → 归一化后全是 100 → 对排序零影响，界面上如实标注"未打分"，绝不拿预制分数冒充
- 升温程度做了**小样本收缩**：`实际倍数 = 1 + (原始倍数 - 1) × min(1, 帖子数/6)`，避免 2 条帖子刚好都在最近时段就被判成"暴涨"
- 未饱和用 `账号数 ÷ 提及数`（反向指标）：人少帖多说明是少数账号在刷屏，人多帖少说明话题刚铺开

```
热度分 = (0.20×互动 + 0.20×账号 + 0.15×提及 + 0.15×新鲜度 + 0.10×升温
          + 0.05×玩梗 + 0.05×搞笑 + 0.05×Drama + 0.05×未饱和) ÷ 权重和    （满分 100）
```

**⚠️ 口径红线：不同窗口的分数不可比。**
"过去 3 天"累积的互动量天然远大于"过去 1 小时"。因此分数只在**同一次检测、同一个窗口内**横向比较；切换窗口时整张榜单重新计算，界面上始终标注"当前窗口：过去 X"。

### 2.6 时间窗口

提供 5 档，界面上做成一排按钮：`1 小时 / 4 小时 / 8 小时 / 24 小时 / 3 天`（满足"至少 2 种"的要求）。
所有窗口都以**真实当前时间**为基准向后回溯，不做"虚拟时钟"。

### 2.6 "最值得 TelyClaw 跟进"的判断依据

"值不值得跟进"必须有一个标准，否则 AI 无从判断。因此**设置页提供一项：TelyClaw 的账号定位**（一句话）。
默认值：`面向独立开发者与早期创业者的 AI 内容增长工具`，用户可随时改。

AI 在推荐 Top 3 时，除了热度分，还要给出**契合度理由**（如"该话题直接命中你的目标人群：独立开发者"）。

---

## 3. 系统架构

```
┌─────────────────────────────────────────────────────────┐
│  浏览器页面（原生 HTML/CSS/JS，零构建）                    │
│  设置  |  监控  |  内容工作台  |  发布记录                 │
└───────────────────────┬─────────────────────────────────┘
                        │ 本地网络请求
┌───────────────────────▼─────────────────────────────────┐
│  Python 服务（FastAPI）                                   │
│  ┌───────────┬───────────┬───────────┬────────────────┐ │
│  │ 账号管理  │ 检测流水线 │ 内容流水线 │ 发布/记录管理  │ │
│  └───────────┴─────┬─────┴─────┬─────┴───────┬────────┘ │
│                    │           │             │           │
│  ┌─────────────────▼───┐ ┌─────▼──────┐ ┌────▼───────┐ │
│  │ 取数层 PostSource   │ │ AI 层      │ │ 发布层     │ │
│  │ · MockSource        │ │ DeepSeek   │ │ Publisher  │ │
│  │ · PasteSource       │ │ 统一错误   │ │ · Mock     │ │
│  │ · XApiSource(预留)  │ │            │ │ · X(预留)  │ │
│  └─────────────────────┘ └────────────┘ └────────────┘ │
│  ┌────────────────────────────────────────────────────┐ │
│  │ 存储层：本地 JSON 文件（账号/设置/草稿/发布记录）    │ │
│  └────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

**核心设计原则：凡是"将来要换成真的"的地方，现在都留一个接口。**
取数、发布、AI 三处都是"接口 + 至少一种实现"，换实现只改配置，不改业务代码。

---

## 4. 目录结构

```
telyclaw/
├── app.py                  # 服务入口，启动后浏览器打开本地地址
├── config.py               # 所有配置项（AI 密钥、取数方式、默认窗口等）
├── requirements.txt        # 依赖清单（仅 2~3 个）
├── core/
│   ├── keywords.py         # 关键词抽取
│   ├── cluster.py          # 话题粗聚类
│   ├── scoring.py          # 热度分计算
│   └── pipeline.py         # 检测流水线编排
├── ai/
│   ├── client.py           # DeepSeek 调用 + 错误规范化
│   ├── prompts.py          # 四类 Prompt 模板
│   └── tasks.py            # 安全检查 / 摘要 / 排名理由 / 角度 / 写稿 / 精选
├── sources/
│   ├── base.py             # 取数接口（抽象）
│   ├── mock_source.py      # 内置模拟数据
│   ├── paste_source.py     # 粘贴导入
│   └── x_api_source.py     # 真 X API（预留，未配置则明确提示）
├── store/
│   └── json_store.py       # 本地 JSON 读写
├── static/
│   ├── index.html
│   ├── style.css
│   └── app.js
└── data/
    ├── accounts.json       # 监测账号
    ├── settings.json       # 设置（定位、密钥、默认窗口、权重）
    ├── seed_posts.json     # 模拟数据模板
    ├── drafts.json         # 草稿
    └── published.json      # 已发布记录
```

---

## 5. 取数层设计（"保留 X API 接口，可无缝切换"）

### 5.1 统一接口

```python
class PostSource:
    def fetch(self, handles: list[str], since, until) -> list[Post]:
        """取回指定账号在时间区间内发布的帖子"""
```

三种实现，靠配置项 `SOURCE` 一行切换：`mock`（默认） / `paste` / `x_api`。

### 5.2 MockSource —— 模拟数据（默认）

**⚠️ 关键设计：时间戳必须相对"现在"动态生成，不能写死绝对时间。**
否则你在晚上演示时选"过去 1 小时"会一条都查不到，整条链路当场空掉。

数据模板里每条帖子只记一个 `offset_minutes`（"距今多少分钟"），生成时再换算成真实时间。分布刻意设计成：

| 距现在 | 帖子条数 | 目的 |
|---|---|---|
| 0~1 小时 | 8~12 条 | 保证"过去 1 小时"窗口有料 |
| 1~4 小时 | 12~18 条 | |
| 4~8 小时 | 10~15 条 | |
| 8~24 小时 | 15~20 条 | |
| 24~72 小时 | 20~25 条 | |
| 合计 | **约 70~90 条** | |

**⚠️ 第二条硬约束：最小窗口也必须能聚出 ≥3 个话题。**
"过去 1 小时"只有 8~12 条帖子，如果这些帖子主题发散，聚类后可能只剩 1~2 个话题，界面会很难看。因此这 8~12 条帖子的内容必须刻意分布在 **3~4 个明确的关键词簇**上。这条要在造数据时专门验证。

其他要求：
- 内容主题覆盖 AI 工具、开发者生态、创业/增长等方向，符合预置账号的人设
- 提供"换一批数据"按钮，可重新随机生成互动数与措辞，避免每次演示内容完全一样
- 界面全程标注"演示数据 · 内容为 AI 模拟生成，非真实言论"

### 5.3 PasteSource —— 手动粘贴/导入

支持两种格式，自动识别：

1. **结构化格式**（JSON 数组）：字段为 作者 / 正文 / 发布时间 / 点赞 / 转发 / 回复
2. **纯文本**：每行一条，用竖线分隔 —— `用户名 | 正文 | 2h | 128`
   （`2h` 这类相对时间会被自动换算；也接受标准日期格式）

导入后同样进入检测流水线，与模拟数据完全等价。

### 5.4 XApiSource —— 真 X API（预留，本次不实现请求逻辑）

接口与返回结构已定义好，将来填入凭证即可启用。已预留的映射关系：

| 需要的能力 | 对应的 X API v2 接口 | 说明 |
|---|---|---|
| 用户名 → 用户 ID | `GET /2/users/by/username/{用户名}` | X 取推文必须先拿到 ID |
| 批量取用户 | `GET /2/users?usernames=a,b,c` | 一次最多 100 个 |
| 取某用户推文 | `GET /2/users/{id}/tweets` | 带 `start_time`/`end_time` 时间过滤 |
| 发推 | `POST /2/tweets` | 发布层用 |

需要预留的配置项：`X_BEARER_TOKEN`（读）、`X_API_KEY` / `X_API_SECRET` / `X_ACCESS_TOKEN` / `X_ACCESS_SECRET`（写）。
未配置时，界面明确提示"尚未接入真实 X 数据"，而不是悄悄失败。

---

## 6. AI 层设计（DeepSeek，写死）

### 6.1 调用方式

- 接口地址：`https://api.deepseek.com`（OpenAI 兼容格式）
- 模型：`deepseek-chat`
- 密钥放在配置文件里，界面设置页可填
- 单次请求超时 30 秒

### 6.2 四类 AI 任务

| 任务 | 输入 | 输出 | 何时触发 |
|---|---|---|---|
| **识别话题 + 摘要** | 全部粗簇（关键词 + 代表帖子 + 统计） | 话题名 + 一句话摘要 + 合并建议 | 点"立即检测"后，一次调用 |
| **排名理由** | Top N 话题的各维度数值 | 每个话题一句"为什么排这个位置" | 榜单生成后，一次调用 |
| **Top 精选推荐** | 榜单 + TelyClaw 账号定位 | 3 个推荐 + 契合度理由 | 榜单生成后，一次调用 |
| **内容角度 + 写稿** | 单个热点 + 定位 | 3 个角度 / 点角度后 3 条帖子 | 用户点击时按需调用 |

**为什么这样切分**：把"一次检测"的 AI 调用压到 3 次以内，避免演示时干等；用户点击才触发的写稿类调用不阻塞主流程。

### 6.3 错误处理（用户明确选择：不做兜底内容，但要友好提示 + 重试）

- 所有 AI 调用统一返回结构化结果：`是否成功 / 错误码 / 一句人话说明 / 是否可重试`
- 常见情况对应的人话文案：

| 情况 | 界面提示 |
|---|---|
| 没填密钥 | "还没填 AI 密钥，去设置页填一下就能用" |
| 网络不通 | "连不上 AI 服务，检查一下网络" |
| 超时 | "AI 这次响应超时了，可以重试" |
| 密钥无效 | "AI 密钥不对，去设置页检查" |
| 返回内容解析失败 | "AI 这次返回的内容没看懂，建议重试" |

- 界面表现为**顶部横幅提示 + 重试按钮**，页面其他部分照常可用，**绝不白屏、绝不卡死**
- 明确规定：**绝不用预写内容冒充 AI 输出**

---

## 7. 页面结构（4 个 Tab）

### Tab 1 · 设置
- 监测账号列表：表格形式，每行有**启用开关**和**删除**按钮
- "添加账号"输入框：填用户名即可添加，支持连续添加
- TelyClaw 账号定位：一句话输入框（AI 判断"值不值得跟进"的依据）
- AI 密钥输入框
- 数据源选择：模拟数据 / 粘贴导入 / 真实 X（未接入）
- 默认时间窗口、默认勾选账号
- 保存后写入本地文件，刷新页面验证不丢

### Tab 2 · 监控（主流程，内部步骤条）
```
① 选择账号  →  ② 选择时间窗口  →  ③ 立即检测  →  ④ 热点榜单  →  ⑤ Top 精选
```
- ① 账号以勾选卡片呈现，可全选/全不选
- ② 一排窗口按钮：`1h / 4h / 8h / 24h / 3d`
- ③ "立即检测"按钮，点击后有加载状态
- ④ 榜单：每张卡片显示 **排名、话题名、热度分、九维数值、风险警告、AI 摘要、排名理由**
- ⑤ Top 精选区：AI 推荐的 3 个，每个带契合度理由，**可取消、可从榜单换入其他热点**

### Tab 3 · 内容工作台
```
选热点  →  看 3 个内容角度  →  点一个角度  →  得到 3 条不同风格帖子  →  选一条编辑  →  发布
```
- 角度卡片：标题 + 为什么这个角度有价值
- 帖子候选：3 种风格（如 犀利观点型 / 数据清单型 / 提问互动型）
- 编辑框：可直接改文字，实时显示字数与 X 的 280 字上限
- "发布"按钮

### Tab 4 · 发布记录
- 草稿 / 已发布 两个状态分组
- 每条显示：正文、关联热点、使用的角度、发布时间
- 已发布的提供"复制"按钮

### 发布交互（模拟发布，但做得像真的）
点"发布" → 弹出**预览卡片**（头像、名字、正文、时间，做成 X 的样子）→ 确认 →
状态变为"已发布" + 写入本地记录 + **文案自动复制到剪贴板**（可以真的去 X 粘贴）。

---

## 8. 端到端演示脚本（照着点就能讲完）

| 步骤 | 操作 | 界面上应该看到 |
|---|---|---|
| 0 | 运行启动命令，浏览器打开本地地址 | 首页加载，默认进"监控"页 |
| 1 | 进设置页 | 已有 6 个预置 KOL 账号 |
| 2 | 现场添加一个新账号，保存，刷新页面 | 新账号还在 → **证明数据真的存住了** |
| 3 | 回监控页，勾选 5~6 个账号，选"过去 24 小时"，点"立即检测" | 加载后出榜单 |
| 4 | 展示榜单 | 6~12 个话题，每个有热度分、九维数值、AI 摘要、排名理由；顶部有安全检查状态条 |
| 5 | 指着第一名念排名理由 | "它排第一是因为：8 条提及、覆盖 3 个账号、升温 2.86 倍、Drama 90 吵得最凶" |
| 6 | 看 Top 精选区 | AI 推荐的 3 个，带契合度理由 |
| 7 | 现场把其中一个换成榜上第 6 名 | **证明人能改 AI 的判断** |
| 8 | 进工作台，点第一个热点 | 出现 3 个内容角度 |
| 9 | 点其中一个角度 | 出现 3 条不同风格的帖子 |
| 10 | 选一条，在编辑框里改几个字 | 字数实时更新 |
| 11 | 点"发布" → 看预览卡片 → 确认 | 状态变已发布，提示"已复制到剪贴板" |
| 12 | 进发布记录页 | 刚才那条在"已发布"里 |
| 13 | （加分）回监控页把窗口切成"1 小时"，重新检测 | 榜单明显变化 → **证明时间窗口真的在起作用** |

---

## 9. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| **最小窗口数据空** | 选 1h 时整条链路空掉，演示当场死 | 模拟数据时间戳相对"现在"生成；且 1h 内刻意分布 3~4 个关键词簇（造数据时专门验证） |
| **AI 调用失败** | 演示中断 | 友好人话提示 + 一键重试；非 AI 部分（取数、聚类、打分、榜单）全部照常展示 |
| **AI 返回格式不稳定** | 解析报错 | Prompt 里强制要求返回固定结构；解析失败时给一次自动重试，再失败才提示 |
| **演示前忘了填密钥** | 一开场就没 AI | 设置页密钥为空时，监控页顶部常驻提示条 + 一键跳转去填 |
| **窗口切换后分数不可比被质疑** | 口径被挑战 | 界面常驻标注当前窗口；文档与界面都写明"分数仅同窗口内可比" |
| **用真实 KOL 名字生成假内容引误解** | 评审质疑 | 界面全程标注"演示数据 · 内容为 AI 模拟生成，非真实言论" |
| **多次 AI 调用导致等待过长** | 演示冷场 | 一次检测压缩到 3 次调用；写稿类按需触发；界面全程有加载状态 |

---

## 10. 验收对照表（逐条对应题面要求）

| # | 题面要求 | 落在哪里 |
|---|---|---|
| 1 | 添加监测用户账号 | 设置页"添加账号"输入框 |
| 2 | 同时检测多个账号 | 监控页账号多选卡片 |
| 3 | 自定义检测时间窗口 1h/4h/8h/24h/3d | 监控页窗口按钮排 |
| 4 | 获取指定窗口内发布的新内容 | 取数层按时间过滤 |
| 5 | 可配置至少 5 个 X 账号 | 预置 6 个 + 可任意添加 |
| 6 | 支持至少 2 种时间窗口 | 提供 5 档 |
| 7 | 可获取或导入指定周期内的帖子 | 模拟数据取数 + 粘贴/导入两种途径 |
| 8 | AI 自动识别多个话题并输出摘要 | 阶段 B：DeepSeek 命名 + 摘要 |
| 9 | 热点排名并解释排名原因 | 九维热度分 + AI 生成排名理由 |
| 9.1 | 内容安全风险过滤 | AI 逐条判定 7 类风险，打显眼标记，话题保留由人决定；未检查时明说 |
| 10 | 选出最值得跟进的 Top 热点 | Top 精选 3 个，AI 推荐 + 用户可改 |
| 11 | 每个重点热点 ≥3 个内容角度 | 工作台角度卡片 |
| 12 | 生成 ≥1 条可直接发布的帖子 | 每个角度出 3 条，选一条编辑后发布 |
| 13 | 基本可操作的 UI | 4 个 Tab，全部按钮/输入框/勾选可交互 |
| 14 | 端到端演示不卡死不报错 | 见第 8 节演示脚本 + 第 6.3 节错误处理 |
| 15 | 保留接口，可无缝切真 X API | 取数层/发布层均为接口 + 多实现，改配置即切换 |

---

## 11. 开发阶段划分（只分阶段，不给工期）

- **阶段一｜骨架跑通**：Python 服务 + 前端页面架子 + 模拟数据 + 能按账号和窗口取出帖子列表
- **阶段二｜热点引擎**：关键词抽取 → 粗聚类 → 热度打分 → 榜单（先用临时话题名，不接 AI）
- **阶段三｜接 AI**：话题命名与摘要、排名理由、Top 精选推荐、内容角度、写稿；错误处理与重试
- **阶段四｜工作台与发布**：角度 → 帖子 → 编辑 → 预览 → 模拟发布 → 发布记录；全量持久化
- **阶段五｜演示打磨**：预置数据调优（重点验证 1h 窗口）、窗口切换效果、异常提示文案、演示脚本走查
